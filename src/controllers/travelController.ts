import { Request, Response, NextFunction } from "express";

export class TravelController {
    constructor() {

    }

    static handleAdd(req: Request, res: Response, next: NextFunction) {

    }

    add(from: Number, to: Number, date: Date) {

    }
}